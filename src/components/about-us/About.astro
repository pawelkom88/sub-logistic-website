---
import "../../styles/onas.css";
import { getCountryCode } from "../../utils/utils";
import { Image } from "astro:assets";
import aboutImage from "../../assets/images/Piotr_Subotowicz.webp";
// import certificateImage1 from "../../assets/images/certyfikat.jpg";
// import certificateImage2 from "../../assets/images/licencja.jpg";

interface Props {
  aboutUs: any;
}

const { aboutUs } = Astro.props;
// const currentLang = getCountryCode(Astro.url.pathname);

---

<section id="content-page-713">
  <div class="cs-container">
    <div class="cs-content">
      <h1 class="cs-title">
        {aboutUs?.h1part1}
        <span class="cs-color">{aboutUs?.h1part2}</span>
      </h1>
      <div class="owner">
        <Image
          src={aboutImage}
          alt="Piotr Subotowicz"
          loading="lazy"
          decoding="async"
          fetchpriority="low"
          sizes="(max-width: 600px) 100vw, 570px"
        />
      </div>
      <h2>{aboutUs?.h2}</h2>
      <p class="cs-text">{aboutUs?.h2Paragraph}</p>

      <h3>{aboutUs?.h3}</h3>
      <p class="cs-text">{aboutUs?.h3Paragraph}</p>

      <h4>{aboutUs?.h4}</h4>
      <p class="cs-text">{aboutUs?.h4Paragraph}</p>

      <h5>{aboutUs?.h5}</h5>
      <p class="cs-text">{aboutUs?.h5Paragraph}</p>

      <h6>{aboutUs?.h6}</h6>
      <p class="cs-text">{aboutUs?.h6Paragraph}</p>

      <br />
      <p class="cs-text">
        <!-- use localized url from translations -->
        <a href={aboutUs?.link?.url}>{aboutUs?.link?.part1}</a
        >, {aboutUs?.link?.part2}
      </p>
      <!--{aboutUs?.documents && (-->
      <!--  <>-->
      <!--    <h4>{aboutUs.documents?.heading}</h4>-->
      <!--    <p class="cs-text">{aboutUs.documents?.intro}</p>-->
      <!--    <ul class="cs-text">-->
      <!--      {aboutUs.documents?.items?.map((doc: any, i: number) => (-->
      <!--        <>-->
      <!--          <li>{doc.title}</li>-->
      <!--          <Picture-->
      <!--            src={i === 0 ? certificateImage2 : certificateImage1}-->
      <!--            alt={doc.alt}-->
      <!--            formats={["avif", "webp"]}-->
      <!--            loading="lazy"-->
      <!--            decoding="async"-->
      <!--            fetchpriority="low"-->
      <!--            sizes="(max-width: 600px) 100vw, 570px"-->
      <!--          />-->
      <!--        </>-->
      <!--      ))}-->
      <!--    </ul>-->
      <!--    <p class="cs-text">{aboutUs.documents?.closingNote}</p>-->
      <!--  </>-->
      <!--)}-->
      <br />
    </div>
    <div class="cs-image-group">
      <Image
        src={aboutImage}
        class="cs-picture"
        alt="Piotr Subotowicz"
        loading="lazy"
        decoding="async"
        fetchpriority="low"
        sizes="(max-width: 600px) 100vw, 570px"
      />
    </div>
  </div>
  <picture class="cs-background">
    <img
      src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images%2FGraphics%2Flarge-waves.svg"
      alt="waves"
      loading="lazy"
      decoding="async"
      aria-hidden="true"
      width="625"
      height="952"
    />
  </picture>
</section>
